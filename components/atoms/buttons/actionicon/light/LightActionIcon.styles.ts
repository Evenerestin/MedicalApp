import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  icon: {
    backgroundColor: "#f5f5f5",
    borderRadius: 50,
    padding: 8,
    borderWidth: 1,
    borderColor: "#1976d2",
  },
});
