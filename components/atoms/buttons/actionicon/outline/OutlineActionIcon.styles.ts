import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  icon: {
    backgroundColor: "transparent",
    borderRadius: 50,
    padding: 8,
    borderWidth: 2,
    borderColor: "#1976d2",
  },
});
