import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    backgroundColor: "#fff",
    borderRadius: 18,
    elevation: 2,
    overflow: "hidden",
  },
});

export default styles;
